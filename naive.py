def init(string, transition)
    M = np.zeros((len(string), len(string)),np.float64)
    for i in range(len(string)):
         
def naive(initial_state,final_state,alphabet, string):
    

    for i in range(len(string)-1):
        for j in range(1,len(string)):
            